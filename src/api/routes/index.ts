// import express from 'express';
// import auth from './auth';
// import user from './user';

// const routes = ({ app, router }: { app: express.Application; router: any }) => {
//   const authRouter = auth(router);
//   const userRouter = user(router);
//   app.use(authRouter);
//   app.use(userRouter);
// };

// export default routes;
